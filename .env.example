# ============================================================
# Buddy Bot Environment Variables
# ============================================================
# Secrets are managed via SOPS + AGE in the alex/secrets repo.
# Do NOT manually create .env — render it from encrypted store:
#
#   make render-env                         # production (default)
#   make render-env ENV=dev                 # dev environment
#   make render-env ENV=staging             # staging environment
#
# Or directly:
#   /home/deploy/work/secrets/secrets/scripts/render-env.sh buddy-bot production -o .env
#
# To edit secrets:
#   cd /home/deploy/work/secrets/secrets
#   sops projects/buddy-bot/secrets/production.enc.yaml
# ============================================================

# --- Secrets (from SOPS-encrypted store) ---
ANTHROPIC_API_KEY=
TELEGRAM_TOKEN=
TELEGRAM_ALLOWED_CHAT_IDS=
OPENAI_API_KEY=
VOYAGE_API_KEY=
TAVILY_API_KEY=
PERPLEXITY_API_KEY=
SPEECHKIT_API_KEY=
YANDEX_FOLDER_ID=

# --- Optional overrides (defaults set in docker-compose.yml) ---
# CLAUDE_MODEL=claude-sonnet-4-5-20250929
# CLAUDE_TIMEOUT=120
# HISTORY_TURNS=20
# DEBOUNCE_DELAY=5
# USER_TIMEZONE=UTC
# SPEECHKIT_LANG=ru-RU
# MAX_VOICE_DURATION=30
# LOG_LEVEL=INFO
